

    // var currInitials;
    // var topScores = [];//local array
  
    // var initInput = document.querySelector("#init");
    // var postItButton = document.querySelector("#postIt");
    // var storedScores = localStorage.getItem("topScores");


    // postItButton.addEventListener("click", function(event) {
    //   event.preventDefault();

    //   currInitials = document.querySelector("#init").value.toUpperCase();
    //   storedScores.push("currInitials");
    //   localStorage.setItem("Scores", topScores);
    // })
    

    function leaderBoard() {
      //creates and appends choices
    for (let i = 0; i < 10; i++ ) {
      console.log("test");
      
    }
  }

    leaderBoard();